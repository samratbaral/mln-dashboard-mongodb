<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/forms.css">
    <link rel="stylesheet" href="/css/auth.css">
</head>

<body>
<%- include('../includes/navigation.ejs') %>

    <main>
        <!-- <img src="/Public/image/favicon.png" width="150" height="100"> -->
        <% if (errorMessage) { %>
            <span class="label danger"> W A R N I N G <center>ERROR MESSAGE</center>                <div class="user-message user-message--error">
                    <center><%= errorMessage %></center>
                </div>
            </span>
        <% } %>
        <!-- <% if (sucessMessage) { %>
            <span class="label success"> S U C E S S
                <div class="user-message user-message--error">
                    <h2><center><%= sucessMessage %></center></h2>
                </div>
            </span>
        <% } %> -->
        <p>
            &emsp;
        </p>
        <center>
        <h1>Welcome User</h1>
        <h3>
            Sign-in to MLN Dashboard
        </h3>

        <p>
            &emsp;
        </p>
    </center>
        <form class="login-form" action="/login" method="POST">
            <div class="form-control">
                <label for="username">Username</label>
                <input 
                class="<%= validationErrors.find(e => e.param === 'username') ? 'invalid' : '' %>"
                type="username" 
                name="username" 
                placeholder="Type your username" 
                id="username"
                value="<%= oldInput.username %>">
                <% if (validationErrors.find(e => e.param === 'username')) { %>
                    <span class="label danger">ERROR ON USERNAME</span>
                <% } %
            </div>
            <div class="form-control">
                <label for="password">Password</label>
                <input
                class="<%= validationErrors.find(e => e.param === 'password') ? 'invalid' : '' %>"
                type="password" 
                name="password" 
                placeholder="Type your password" 
                id="password"
                value="<%= oldInput.password %>">
                <% if (validationErrors.find(e => e.param === 'password')) { %>
                    <span class="label danger">ERROR ON PASSWORD</span>
                <% } %
            </div>
            <div>
                <a href="/reset">Reset Password</a>
            </div>
            &emsp;
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <center><button class="btn" type="submit"> Login </button></center>
        </form>
        <!-- <div class="centered">
            <a href="/reset">Reset Password</a>
        </div> -->
        <!-- <form action="/reset" method="POST" >
            <ul>
                <input 
                type="hidden" 
                name="_csrf" 
                value="<%= csrfToken %>">
                <button type="submit">Reset Password</button>
            </ul>
        </form> -->
        <!-- <form class="login-form" action="/signup" method="POST" >
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button class="btn" type="submit">Register</button>
        </form> -->
    </main>
<%- include('../includes/end.ejs') %>